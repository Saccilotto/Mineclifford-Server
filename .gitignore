# Python venv directories
venv/

# Local .terraform directories
**/.terraform/*

# Go workspace files
*.mod
*.sum

# Compiled Python files
*.pyc
*.pyo
*.pyd
__pycache__/
*.so
*.egg
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/

# Database files
data/
*.db
*.sqlite
*.sqlite3

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Ignore Terraform plan output files (binary, environment-specific, not for VCS)
*.plan
*tfplan*
tf.plan

# Ignore CLI configuration files
.terraformrc
terraform.rc

*.json

# Log files (generated by minecraft-ops.sh and other scripts)
*.log
logs/

# Archive/world files (generated, large, not for VCS)
*.zip
*.tar.gz
world_imports/

# SSH keys (sensitive — never commit)
ssh_keys/

# Generated IP/host config (environment-specific)
static_ip.ini
